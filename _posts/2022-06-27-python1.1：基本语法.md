---
layout: post
read_time: true
show_date: true
title: "python基本语法1.1"
date: 2022-06-27
img: posts/python/python_main.jpeg
tags: [python]
category: opinion
author: 张大jio
description: "字面量、变量、标识符、数据类型、类型检查、运算符"
---

句末没有分号，print中是单双引号都可以
在python中默认输出是换行的，要想不换行需要在变量末尾加上 end=""
任意位置：ctrl+/：注释；ctrl+回车：换行


# 1.字面量和变量、标识符

字面量就是一个一个的值，比如：1，2，3，4，5，6，‘HELLO’；字面量所表示的意思就是它的字面的值，在程序中可以直接使用字面量

变量（variable）变量可以用来保存字面量，并且变量中保存的字面量是不定的；变量本身没有任何意思，它会根据不同的字面量表示不同的意思
As neural networks are used more and more in the creative process, text, images and even music are now created by AI, but who owns the copyright for those works?
开发时，很少直接使用字面量，都是将字面量保存到变量中，通过变量来引用字面量

## 1.1变量

Python中使用变量，不需要声明，直接为变量赋值即可：

```python
a = 10
```

不能使用没有进行过赋值的变量，如果使用没有赋值过的变量，会报错 NameError: name 'b' is not defined 

```python
print(b)
```

Python是一个动态类型的语言，可以为变量赋任意类型的值，也可以任意修改变量的值

```python
a = 'hello'
print(a)
```
## 1.2标识符
在Python中所有可以自主命名的内容都属于标识符：变量名、函数名、类名
标识符必须遵循标识符的规范

>1.标识符中可以含有字母、数字、_，但是不能使用数字开头：a_1 _a1 _1a
   2.标识符不能是Python中的关键字和保留字，也不建议使用Python中的函数名作为标识符,因为这样会导致函数被覆盖
   
   3.命名规范：在Python中注意遵循两种命名规范：
> 下划线命名法：所有字母小写，单词之间使用_分割
> `max_length min_length hello_world xxx_yyy_zzz`
>帕斯卡命名法（大驼峰命名法）：首字母大写，每个单词开头字母大写，其余字母小写
>                `MaxLength MinLength HelloWorld XxxYyyZzz`

       
   如果使用不符合标准的标识符，将会报错 SyntaxError: invalid syntax
## 练习
尝试自己定义几个变量（复杂一些，尝试不同的命名法），然后打印一些变量

```python
Student_Age=15
Student_sex='男'
人数=3
print('学生人数',人数,Student_Age,Student_sex)#学生人数 3 15 男
```

# 2.数值
在Python数值分成了三种：整数、浮点数（小数）、复数
### 1.3.1整数
Python中所有的整数都是int类型
Python中的整数的大小没有限制，可以是一个无限大的整数
如果数字的长度过大，可以使用下划线作为分隔符
```python
c = 9999_9999_9999
c = 999999999999999999999999999999999999999999999 ** 100
**表示次幂
```
10进制的数字不能以0开头
其他进制整数

> #其他进制的整数，只要是数字打印时一定是以十进制的形式显示的
 二进制 0b开头
`c = 0b10` # 二进制的10
#八进制 0o开头
`c = 0o10`
#十六进制 0x开头
`c = 0x10`

可以通过运算符来对数字进行运算，并且可以保证整数运算的精确

```python
c = -100
c = c + 3
```

### 1.3.2浮点数
浮点数（小数），在Python中所有的小数都是float类型

```python
c = 1.23
c = 4.56
```

对浮点数进行运算时，可能会得到一个不精确的结果
对于精确的要求，最好不要浮点直接计算
c = 0.1 + 0.2 # 0.30000000000000004

# 3.字符串
字符串(str)
字符串用来表示一段文本信息，字符串是程序中使用的最多的数据类型
在Python中字符串需要使用**引号（单双都行，但不能混用）**引起来（Java双引号）

**相同的引号之间不能嵌套**

```python
s1 = '子曰:"学而时习之，乐呵乐呵！" '
s2 = "子曰:'学而时习之，乐呵乐呵！' "
#s3 = "子曰:"学而时习之，乐呵乐呵！" "错误
```
## 3.1长字符串
### 3.1.1不换行
单引号和双引号不能跨行使用
注意反斜杠

```python
s = '锄禾日当午，\
汗滴禾下土，\
谁知盘中餐，\
粒粒皆辛苦'
print(s)
```
打印结果：

> 锄禾日当午，汗滴禾下土，谁知盘中餐，粒粒皆辛苦

### 3.1.2换行
使用三重引号来表示一个长字符串

>  ' ' ' XXX ' ' '或者" " " XXX " " "

三重引号可以**换行**，并且会**保留字符串中的格式**

```python
s = '''锄禾日当午，
汗滴禾下土，
谁知盘中餐，
粒粒皆辛苦'''
print(s)
```

打印结果：

> 锄禾日当午，
>  汗滴禾下土，
>   谁知盘中餐， 
>   粒粒皆辛苦
## 3.2转义字符
\' 表示'
\" 表示单纯双引号"
\t 表示制表符 
\n 表示换行符
\\ 表示反斜杠
\uxxxx 表示Unicode编码

```python
s = "子曰:\"学而时习之，\\\\n乐呵乐呵！\""
```
结果为：**子曰:"学而时习之，\\n乐呵乐呵！"**
\uxxxx 打一些键盘无法输入的图形
```python
s = '\u0040'
```
结果为@
## 3.3字符串相加
如果将两个字符串进行相加，则会自动将两个字符串拼接为一个

```python
a = 'abc' + 'haha' + '哈哈'
a=123
print("a = "+a)#这种写法在Python中不常见，字符串+数字报错
print('a = ',a)#常用，因为没有进行+运算
```
**字符串不能和其他的类型进行加法**运算，如果做了会出现异常 TypeError: must be str, not int
## 3.4占位符
在创建字符串时，可以在字符串中指定占位符
**%s 在字符串中表示任意字符
%f 浮点数占位符
%d 整数占位符**

```python
str='hello %s'
print(str)
```
输出结果为：hello %s
### 3.4.1占位符填充
**填充占位符**：**引号外%+"填充字符"**（也可以是单引号）

```python
str='hello %s' % '孙悟空'
print(str)
```
输出结果为：hello 孙悟空
**多个占位符填充：引号外%+("填充字符1"，"填充字符1"+……)**

占位符是按顺序填充
```python
str='%s hello %s' % ('你好','孙悟空')
```
### 3.4.2占位符填充长度
![在这里插入图片描述](https://img-blog.csdnimg.cn/3594f2c16cb84b06980ea91a3f7c088c.png)


**%2s**表示填充的字符串最小2个字符，不够用空格补位
**%3.5s**表示填充的字符串最少3个字符，最多5个字符，不够用空格补位
**%.1s**表示填充的字符串最多1个字符，不够用空格补位

```python
#你好123
str='你好%s'%123
print(str)
```
与+不一样，整型、浮点型也能填充
%f：**%.2f**保留两位小数，还自动四舍五入

```python
#你好123.46
str='你好%.2f'%123.456
print(str)
```
%d:

```python
a=123
print('a = %s'%a)
```
## 3.5格式化字符串
可以通过在字符串前添加一个f来创建一个格式化字符串

```python
c=f'hello'
```
在格式化字符串中可以直接嵌入变量

```python
c = f'hello {a} {b}'
```
{a}：表示a是一个变量，{a}会被a的值取代

```python
a=123
b=456
c = f'hello {a} {b}'

print(f'a = {a}')#a = 123
print(c)#hello 123 456
```
## 3.6打印输出
1.+连接：print("a = "+a)
2. print('a = ',a)
3. 占位符：print('a = %s' %a)
4. 格式化字符串直接传变量：print(f'a = {a}')

## 练习 
创建一个变量保存你的名字，然后通过四种格式化字符串的方式
在命令行中显示，欢迎 xxx 光临！

```python
name='张三'
print('欢迎'+name+"光临")
print('欢迎',name,'光临')
print('欢迎%s光临'%name)
print(f'欢迎{name}光临')
```
## 3.7字符串复制
用乘法*，*2表示变为原来的两倍（复制一次）

```python
a = 'abc'
a = a * 2
print(a)
```
结果abcabc
如果将字符串和数字相乘，则解释器会将字符串重复指定的次数并返回

# 4.布尔型和空值
## 4.1布尔型
true和false，不加引号，主要用来做逻辑判断
布尔值实际上也属于整型，True就相当于1，False就相当于0

## 4.2空值
None（空值）：专门用来表示不存在
# 5.类型检查

```python
a = 123 # 数值
b = '123' # 字符串

# print('a =',a)
# print('b =',b)、
```
a、b打印出来的结果相同，无法通过结果区分数据类型
通过类型检查，可以检查只能值（变量）的类型
**type()用来检查值的类型**，该函数会将检查的结果作为返回值返回，可以通过变量来接收函数的返回值

```python
a=123
b='123'
print(type(a))
print(type(b))

```
结果为：
<class 'int'>
<class 'str'>

# 6.对象object
- Python是一门面向对象的语言
    - 一切皆对象！
    - 程序运行当中，所有的数据都是存储到内存当中然后再运行的！
    - **对象就是内存中专门用来存储指定数据的一块区域**
    - **对象实际上就是一个容器，专门用来存储数据**
    - 之前学习的数值、字符串、布尔值、None都是对象
    - 参考图1
![在这里插入图片描述](https://img-blog.csdnimg.cn/8fa72bbeadc14d8eb227aecd46c4b124.png)

## 6.1对象的结构
每个对象中都要保存三种数据
id（标识）

> id用来标识对象的唯一性，每一个对象都有唯一的id 
> 对象的id就相当于人的身份证号一样 可以通过**id()函数来查看对象的id**
> `print(id(a))`
> id是由解析器生成的，在CPython中，**id就是对象的内存地址** 
> 对象一旦创建，则它的id永远不能再改变

        
   type（类型）

>  类型用来标识当前对象所属的类型    比如：int str float bool 。。。    
> 类型决定了对象有哪些功能   
> 通过**type()函数**来查看对象的类型    
> Python是一门强类型的语言，对象一旦创建类型便不能修改

value（值）

> 值就是对象中存储的具体的数据 
> 对于有些对象值是可以改变的 
> 对象分成两大类，可变对象 不可变对象： 
> 	可变对象的值可以改变
> 	不可变对象的值不能改变，之前学习的对象都是不可变对象


![在这里插入图片描述](https://img-blog.csdnimg.cn/bb5d4683c6c1415ca0e46b2dfeaa7248.png)

	
   ## 练习：
   尝试自己画一下对象的内存结构。  
## 6.2对象和变量

> 对象并没有直接存储到变量中，在Python中变量更像是给对象起了一个别名（a==123就是把变脸取名为a）
> **变量中存储的不是对象的值，而是对象的id（内存地址）**， 当我们使用变量时，实际上就是在通过对象id在查找对象
> 变量中保存的对象，只有在为变量重新赋值时才会改变 变量和变量之间是相互独立的，修改一个变量不会影响另一个变量

 ![在这里插入图片描述](https://img-blog.csdnimg.cn/a28b2a2f5e6241d6b2ee6a2e4e94b233.png)
## 6.3对类型转换
所谓的类型转换，将一个类型的对象转换为其他对象
类型转换不是改变对象本身的类型，而是根据当前对象的值创建一个新对象
，如果希望改变原来的值，则要重新赋值`a=int(a)`
类型转换四个函数 int() float() str() bool()
### 6.3.1 int()
int() 可以用来将其他的对象转换为整型
  规则：
    布尔值：True -> 1   False -> 0
    浮点数：**直接取整**，省略小数点后的内容
    字符串：**合法**的整数字符串“123”，直接转换为对应的数字
            如果**不是一个合法的整数字符串**“11.5”，则报错 ValueError: invalid literal for int() with base 10: '11.5'
    对于其他**不可转换**为整型的对象（String等），直接抛出异常 ValueError
### 6.3.2 float()
float() 和 int()基本一致，不同的是它会将对象转换为浮点数
### 6.3.3 str()
str() 可以将对象转换为字符串
   True -> 'True'
   False -> 'False'
   123 -> '123' 
### 6.3.4 bool()
bool() 可以将对象转换为布尔值，任何对象都可以转换为布尔值
    
  规则：
    对于所有表示空性的对象都会转换为False，其余的转换为True
        哪些表示的空性：**0** 、 **None** 、 **''**或**“”**（空串）

# 7.运算符

## 7.1算术运算符
加法+：相加，拼串
减法-：不能“减串”
乘法*：如果将字符串和数字相乘，则会对字符串进行复制操作，将字符串重复指定次数
除法/：运算时结果总会返回一个浮点类型
整除// ：只会保留计算后的整数位，总会返回一个整型，不四舍五入
幂运算** ，求一个值的几次幂
取模%，求两个数相除的余数

在对浮点数（20.0）做算术运算时，结果也会返回一个浮点数
## 7.2赋值运算符
= 可以将等号右侧的值赋值给等号左侧的变量

## 7.3关系运算符（关系运算符）
关系运算符用来比较两个值之间的关系，总会返回一个布尔值
如果关系成立，返回True，否则返回False

> 	**\>** 比较左侧值是否大于右侧值
  >**=** 比较左侧的值是否大于或等于右侧的值
  **<** 比较左侧值是否小于右侧值
  **<=** 比较左侧的值是否小于或等于右侧的值
  **==** 比较两个对象的值是否相等
  **!=** 比较两个对象的值是否不相等
 **相等和不等**比较的是**对象的值**，而不是id
  **is** 比较两个对象是否是**同一个对象**，比较的是对象的**id**
  **is not** 比较两个对象是否**不是同一个对象**，比较的是对象的id

在Python中可以对两个字符串进行大于（等于）或小于（等于）的运算
**当对字符串进行比较时，实际上比较的是字符串的Unicode编码**
比较两个字符串的Unicode编码时，是**逐位比较**的
利用该特性可以对字符串按照字母顺序进行排序，但是对于中文来说意义不是特别大
**注意：如果不希望比较两个字符串的Unicode编码，则需要将其转换为数字然后再比较**

```python
print(int('123')>int('4'))
```

## 7.4逻辑运算符
### 7.4.1逻辑非
 not ：对符号右侧的值进行非运算
> 对于布尔值，非运算会对其进行取反操作，True变False，False变True 对于非布尔值，非运算会先将其转换为布尔值，然后再取反

### 7.4.2逻辑与
and：对符号两侧的值进行与运算

> 只有在符号两侧的值都为True时，才会返回True，只要有一个False就返回
> False与运算是找False的
> Python中的与运算是短路的与，如果第一个值为False，则不再看第二个值

### 7.4.3逻辑或
or：对符号两侧的值进行或运算

> 或运算两个值中只要有一个True，就会返回True 
> 或运算是找True的
> Python中的或运算是短路的或，如果第一个值为True，则不再看第二个值

### 练习：
尝试一下对布尔值进行三种逻辑运算 
尝试对非布尔值进行三种逻辑运算，并观察返回的结果

```python
print(True and False)
print(True or False)
print(not True)

print(not None)
print(0 or 123)
print(0 and 123)
```
结果
False
True
False
True
123
0
### 7.4.4非布尔值的与或运算
当我们对非布尔值进行与或运算时，Python会将其当做布尔值运算，最终会返回原值
    
与运算的规则

> 与运算是找False的，如果第一个值是False，则不看第二个值 
> 如果第一个值是False，则直接返回第一个值，否则返回第二个值

或运算的规则

> 或运算是找True的，如果第一个值是True，则不看第二个值 
> 如果第一个值是True，则直接返回第一个值，否则返回第二个值


## 7.5条件运算符（三元运算符）
语句1 if 条件表达式 else 语句2

执行流程：

> 条件运算符在执行时，会先对条件表达式进行求值判断 
> 如果判断结果为True，则执行语句1，并返回执行结果
> 如果判断结果为False，则执行语句2，并返回执行结果

```python
print('a的值比较大！') if a > b else print('b的值比较大！')
```

### 练习：
现在有a b c三个变量，三个变量中分别保存有三个数值，请通过条件运算符获取三个值中的最大值
 

```python
a=15
b=15
c=19
max=a if a>=b      else b#不为max=b
max=c if c>=max    else max
print(max)
```

```python
max = a if (a > b and a > c) else (b if b > c else c) # 不推荐这么使用
max = a if (b < a > c) else (b if b > c else c)
```

Java：条件语句?为真时语句：为假时语句
## 7.6运算符优先级
和数学中一样，在Python运算也有优先级，比如先乘除后加减
如果优先级一样则自左向右计算
在开发中如果遇到优先级不清楚的,则可以通过小括号来改变运算顺序



>不要去记
> 括号：() 
> 幂运算：** 
> 按位取反：~ 
> 正号、负号：+、- 
> 乘、除、取模、取整除：* 、/、 %、 //
>  加、减：+ 、-
> 右移、左移：>> 、<< 
> 按位 “与”：& 
> 按位 “异或”，按位 “或”：^ 、| 
> 比较运算符：<= 、< 、>、 >=
> 等于、不等于：==、!= 
> 赋值运算符：=、%=、/=、//=、-=、+=、*=、**= 
> 身份运算符：is、is not
> 成员运算符：in、not in 
> 逻辑运算符：and or not

虽然 Python 运算符存在优先级的关系，但写程序时不建议写很长的表达式，建议写程序时，遵守以下两点原则：

尽量不要把一个表达式写的过长过于复杂，如果计算过程的确需要，可以尝试将它拆分几部分来写。

尽量多使用 () 来控制运算符的执行顺序，使用 () 可以让运算的先后顺序变得十分清楚。




